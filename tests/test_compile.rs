use lambda::codegen;
use lambda::parser;

#[test]
fn test_dummy() {
    // let expr: parser::ParseResult = "x y z".parse();
    // assert!(expr.is_ok());
    // let expr = expr.unwrap();
    // println!("{}", expr);
    // println!("{}", codegen::compile(expr));
}
